// tenart operator
// Guard Operator
// default operator
// Simplifies 
// Use


// Ternay operator
// Quick one line if-else

    // syntax = Conditon ? Truevalue : falsevalue

    // let umar = 31
    // let result;
    // if (umar > 18 ){
    //     result = 'adult';
    // }else {
    //     result = 'kid';
    // }
    // console.log(result);

    // so in instead of this we use the tenary operator

    // let umar = 32;
    // let result = umar > 18 ? 'Adult' : 'kid'
    // console.log(result);
    

    //   Guard operator
    //   use when a flaback value is needed

    // let length = 6;
    // let finallength = length || 18;
    // console.log(finallength);
    // so they print the length is 6 because run direct
    
    // let length ;
    // let finallength = length || 18;
    // console.log(finallength);
    // they print the 18 because the length is undefine
    